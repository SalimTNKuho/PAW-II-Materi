<!DOCTYPE html>
<html>
<head>
  <title>Minimarket</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
<div class="container mt-5">
  <h1 class="mb-4">Daftar Produk</h1>
  <a href="/add" class="btn btn-success mb-3">Tambah Produk</a>
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Nama Barang</th>
        <th>Qty</th>
        <th>Harga</th>
        <th>Diskon (%)</th>
        <th>Subtotal</th>
      </tr>
    </thead>
    <tbody>
      <% let total = 0; %>
      <% products.forEach(p => { 
           let discountRate = (p.discount || 25) / 100;
           let initialSubtotal = p.qty * p.price;
           let discountAmount = initialSubtotal * discountRate;
           let finalSubtotal = initialSubtotal - discountAmount;
           total += finalSubtotal;
      %>
        <tr>
          <td><%= p.name %></td>
          <td><%= p.qty %></td>
          <td>Rp<%= p.price.toLocaleString() %></td>
          <td><%= p.discount || 25 %>%</td>
          <td>Rp<%= finalSubtotal.toLocaleString() %></td>
        </tr>
      <% }) %>
    </tbody>
    <tfoot>
      <tr>
        <th colspan="4" class="text-end">Total</th>
        <th>Rp<%= total.toLocaleString() %></th>
      </tr>
    </tfoot>
  </table>
</div>
</body>
</html>
