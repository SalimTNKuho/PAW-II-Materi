<!-- untuk dijalankan didalam terminal -->
CTRL+` → disebelah kiri "1"

<!-- untuk buat folder utuh baru -->
mkdir = "buat folder
mkdir controller
mkdir models


<!-- buat express baru -->
express [nama folder masukkan/operasional]
cd [nama folder masukkan/operasional]
npm init -y
npm install
npm install -g express-generator
npm install express ejs body-parser
npx express-generator
npm update <!-- pastikan diatas sudah dijalankan dulu -->
npm install <!-- lakukan lagi kalau ada kesalahan!!! -->

npm install express cors <!-- untuk backend??? -->

npm install dotenv express mongoose
file ".env"  dan "node_module" <!-- jangan di push -->

npm install bcryptjs dotenv express mongoose nodemon <!-- untuk install module khusus server -->

npx create-react-app my-spa <!-- untuk buat react (file) baru -->
<!-- struktur folder react  --> ↓↓↓
my-spa/ [folder]
    src/ [folder]
        App.js [file]
        Home.js [file]
        About.js [file]
        Contact.js [file]
        index.js [file]
    package.json [file - dari react otomatis buat]

POST  http://localhost:5000/api/users/register
x-www-form-urlencoded

ada 2 framework yang digunakan:
1. MongoDB.json [node.js + express] (Back-End)
2. Angular.json [data.json] (Front-End)

<!-- Menginstall Angular -->
npm install -g @angular/cli
node -v
ng version → #menggunakan command "ng" untuk Angular

<!-- Membuat proyek baru Angular -->
ng new [nama-proyek]
pilih yang "a" (all) → yang ada pertanyaan AI-nya
→> note: file utama ada di folder "src"
cd [nama-proyek] → pastikan ini dilakukan sebelum melakukan "ng serve"!!!
ng serve → utk mengecek server
ng generate component hello // ng g c hello

<!-- install bootstrap dalam angular -->
cd [nama-proyek] → pastikan lakukan ini terdahulu
npm install bootstrap → supaya file "angular.json" terlihat
lalu pilih pilihlah file "angular.json"
kemudian masukkan dalam line 82 dibawah "style" (diatas "src/styles.css") dengan:
    "node_modules/bootstrap/dist/css/bootstrap.min.css",
lalu ketikkan "scripts" dibawa "style" tersebut, lalu masukkan:
    "node_modules/jquery/dist/jquery.min.js",
    "node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"
ng generate component minimarket_app → komponen

<!-- struktur folder proyek -->
src/ → file utama proyek
app/ → komponen
assets/ → gambar, icon, teks
angular.json/ → front-end proyek
mongodb.json/ → back-end proyek

<!-- langkah penghubung backend dan frontend -->
[Backend] → untuk data
mkdir backend
cd backend
npm init -y
npm install express cors
node server.js

↓ download materi di "pertemuan 18" dalam spon, lalu masukkan file tersebut kedalam backend:
data.json
server.js

↓ lalu:
npm install express
node server.js
note: localhost:3000/api/pegawai → untuk melihat data

[Frontend]
npm install -g @angular/cli → bagi belum install
ng new frontend
ng serve